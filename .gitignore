.icp/cache/
.mops/
node_modules/
.DS_Store
*.log
*.wasm
*.wasm.gz
.env
.env.*
!*.env.example
.mops-cache/

# Replica test harness: the downloaded pocket-ic server and didc, and the
# lockfile npm writes next to the pinned versions in setup.mjs.
.pocket-ic/
tools/pocket-ic/package-lock.json

# Documentation site: site-src/ is staged from the repo, site/ is the build output.
site/
site-src/
.venv-docs/
